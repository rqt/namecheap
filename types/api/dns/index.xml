<types namespace="_namecheap">
  <type name="Host" desc="The information about the host.">
    <prop number name="HostId">
      UniqueID of the host records, e.g., `151362331`.
    </prop>
    <prop string name="Name">
      The domain or subdomain for which host record is set, e.g., `*`, `＠`.
    </prop>
    <prop string name="Type">
      The type of host record that is set, e.g., `A`.
    </prop>
    <prop string name="Address">
      The value that is set for the host record (IP address for A record, URL for URL redirects, etc.), e.g., `10.28.121.54`.
    </prop>
    <prop number name="MXPref">
      MXPreference number, e.g., `10`.
    </prop>
    <prop number name="TTL">
      TTL value for the host record, e.g., `1799`.
    </prop>
    <prop boolean name="IsDDNSEnabled">
      false
    </prop>
    <prop boolean name="IsActive">
      true
    </prop>
    <prop string name="FriendlyName">
      ""
    </prop>
    <prop string name="AssociatedAppTitle">
      ""
    </prop>
  </type>
</types>