<types>
  <type name="Options" desc="Options for the NameCheap client.">
    <prop string name="user">
      The username required to access the API.
    </prop>
    <prop string name="key">
      The password required used to access the API.
    </prop>
    <prop string name="ip">
      The IP address of the client accessing the application (End-user IP address).
    </prop>
    <prop boolean name="sandbox" default="false">
      Whether to use the sandbox version of the API.
    </prop>
  </type>
  </type>
</types>