export {}
/**
 * @typedef {import('./users').GetPricing} GetPricing
 * @typedef {import('./users').Pricing} Pricing
 * @typedef {import('./domains').GetList} GetList
 * @typedef {import('./domains').GetInfo} GetInfo
 * @typedef {import('./domains').DomainInfo} DomainInfo
 * @typedef {import('./domains').DomainCheck} DomainCheck
 * @typedef {import('./domains').Check} Check
 * @typedef {import('./domains').Create} Create
 * @typedef {import('./domains').RegistrationResult} RegistrationResult
 * @typedef {import('./domains').Domain} Domain
 * @typedef {import('./address').Address} Address
 * @typedef {import('./address').AddressDetail} AddressDetail
 * @typedef {import('./dns').Host} Host
 */
